# Environment Configuration Template
# Copy this file to .env.local, .env.dev, or .env.prod and fill in your values

# Application Settings
APP_NAME=Growth Tracker API
ENV=local  # Options: local, dev, prod

# CORS Settings (JSON array format)
CORS_ORIGINS=["*"]

# Mail Settings (Optional - Leave empty if not using)
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=noreply@example.com
MAIL_SERVER=
MAIL_PORT=587

# Note: Database connection strings are managed in config.ini
# The application will automatically load the appropriate database
# configuration based on the ENV variable set above
